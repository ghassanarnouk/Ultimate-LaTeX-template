% arara: pdflatex if !unchanged('tex')
% arara: pdflatex if !unchanged('tex')
% arara: clean: { extensions: [ aux, log ] } if exists('aux') || exists('log')

\documentclass{article}
\begin{document}
    \section{Test}
    Hello, World!
\end{document}